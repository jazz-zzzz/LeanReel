# Product

## Register

product

## Users

LeanReel 面向维护个人媒体库、NAS 片库和本地视频归档的用户。用户通常在 Windows 桌面环境里批量扫描影片目录，判断哪些文件值得压缩，哪些文件应该保持原样。他们理解编码、HDR、码率、音轨和字幕等概念，但不希望每次都手写 FFmpeg 命令或逐个阅读 FFprobe 输出。

核心使用情境是整理已有媒体库，而不是创作新媒体。用户坐在桌面电脑前，面对大量文件列表，需要快速过滤风险、确认收益、选择策略并启动队列。

## Product Purpose

LeanReel 的目标是做一个可解释的媒体库瘦身决策工具。它扫描目录、缓存媒体元数据、识别编码和 HDR 类型、自动推荐安全策略，并把转码任务交给本地 FFmpeg 执行。

成功不是盲目压缩最多文件，而是在默认设置下避免误伤优质片源。HEVC/H.265、HDR10、HDR10+ 和 Dolby Vision 被视为优质或高风险片源，默认完全跳过。可处理对象主要是 SDR 的 H.264、MPEG-2、VC-1 和高码率旧编码片源。

## Brand Personality

冷静、专业、克制。

界面应像一张清楚的工作台：信息密度高，但不紧张；参数专业，但不炫技；风险状态醒目，但不制造焦虑。文案要直接说明发生了什么，少用营销词，避免“视觉无损”这类无法对所有片源承诺的绝对说法。

## Anti-references

- 不要像营销落地页。LeanReel 是工具，不需要大标题、宣传卡片、装饰插画或英雄区。
- 不要像播放器皮肤。界面不负责播放内容，不应使用影院式海报墙或沉浸式媒体封面布局。
- 不要像压制组脚本前端。参数可以专业，但不能把用户推入命令行式噪声。
- 不要使用泛 AI 工具审美：紫蓝渐变、玻璃卡片、发光边框、夸张空状态。
- 不要把危险操作藏在中性文案里。跳过、失败、取消、覆盖、删除都要清楚。

## Design Principles

1. **保护优质片源优先。** HEVC/H.265、HDR10、HDR10+ 和 Dolby Vision 默认不处理，界面必须把这个边界显示出来。
2. **技术名优先。** 策略使用 `x265 HEVC CRF 20 标准转码`、`NVENC HEVC CQ 26 快速转码` 这类中文技术名，避免“轻量、均衡、极限”这类含糊名称。
3. **决策可解释。** 每个自动推荐或跳过状态都应能回答：为什么是这个策略，为什么不处理这个文件。
4. **高密度但可扫描。** 文件列表是主界面核心，列宽、状态颜色、排序、筛选和勾选行为必须服务批量判断。
5. **危险动作显式化。** 开始编码、取消、删除、覆盖、保留临时文件等动作需要清晰状态和可恢复预期。

## Accessibility & Inclusion

目标至少满足桌面应用层面的 WCAG AA 对比度意图。状态不能只依赖颜色，必须同时有文字、图标或可读标签。控件字号不得低于 11px，主数据文本建议 13px。键盘焦点、禁用态和选中态必须可见。

动效应保持克制，只用于状态反馈和展开收起。尊重系统字体和用户显示缩放，不使用负字距或依赖极细字重。中文、英文、技术缩写混排时，要避免列内文字挤压和截断误读。
