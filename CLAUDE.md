# 语言偏好
请始终使用简体中文回复。

# 远程同步检查
该项目已推送到远端仓库。在执行任何代码审查、代码阅读或修改之前，必须先检查本地是否与远端一致：

1. 执行 `git fetch origin` 获取远端最新状态
2. 对比本地分支与 `origin/master`（或对应远程分支）的差异
3. 如果本地落后于远端，先告知用户，确认是否需要拉取最新代码
4. 切勿在本地过时代码的基础上进行审查或修改

# TDD 防逃逸规则

编写测试时，必须确保测试无法被硬编码或空实现欺骗。以下规则强制执行：

1. **每个功能最少 2 个正向用例**，参数不能全部使用平凡值（0、空字符串、空列表、默认值）。至少一个用例使用非平凡的真实数据。

2. **涉及过滤/分支的逻辑，必须先测试"有数据被过滤掉"的场景**。例如：测试评论音轨过滤时，输入数据中必须包含评论音轨，断言它确实被排除。

3. **Mock 外部依赖时必须用 `assert_called_once_with` / `assert_called_with` 验证调用参数**。不能只验证返回值，必须验证副作用。

4. **禁止使用 `return_value=[]` 或 `return_value=""` 作为 mock 的默认返回值**。mock 应返回与真实场景相符的数据。

5. **写完测试后自检**：如果把实现代码替换为 `return 硬编码值`，这个测试能发现吗？如果发现不了，测试不够严格，需要补充用例。

6. **避免精确匹配陷阱**：测试应验证业务不变性（排序关系、范围、类型）而非具体字符串格式。例如验证 `"KB" in result` 而非 `result == "1.0 KB"`。

7. **空输入测试必须单独编写**，不能替代正常数据测试。空输入测试验证的是边界行为，正常数据测试验证的是核心逻辑。
