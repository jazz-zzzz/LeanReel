# 语言偏好
请始终使用简体中文回复。

# 远程同步检查
该项目已推送到远端仓库。在执行任何代码审查、代码阅读或修改之前，必须先检查本地是否与远端一致：

1. 执行 `git fetch origin` 获取远端最新状态
2. 对比本地分支与 `origin/master`（或对应远程分支）的差异
3. 如果本地落后于远端，先告知用户，确认是否需要拉取最新代码
4. 切勿在本地过时代码的基础上进行审查或修改
